---
slug: first-blog-post
title: First Blog Post
authors:
  name: 0xAutist
  title: Janitor at 4chan
  url: https://twitter.com/Tesla809
  image_url: https://cointelegraph.com/magazine/wp-content/uploads/2021/03/unnamed1.png
tags: [hola, docusaurus]
---

import Highlight from "../src/components/Highlight";
import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import remarkMath from "remark-math";

Lorem ipsum dolor sit amet, consectetur adipiscing elit. Pellentesque elementum dignissim
ultricies. Fusce rhoncus ipsum tempor eros aliquam consequat. Lorem ipsum dolor sit
amet

**Simple KaTex**

Let $f\colon[a,b]\to\R$ be Riemann integrable. Let $F\colon[a,b]\to\R$ be
$F(x)=\int_{a}^{x} f(t)\,dt$. Then $F$ is continuous, and at all $x$ such that
$f$ is continuous at $x$, $F$ is differentiable at $x$ with $F'(x)=f(x)$.

$$
I = \int_0^{2\pi} \sin(x)\,dx
$$

<Highlight color="#25c2a0">Docusaurus green</Highlight>

<Tabs>
  <TabItem value="apple" label="Apple" default>
    This is an apple 🍎
  </TabItem>
  <TabItem value="orange" label="Orange">
    This is an orange 🍊
  </TabItem>
  <TabItem value="banana" label="Banana">
    This is a banana 🍌
  </TabItem>
</Tabs>

**code blocks**

<Tabs>

<TabItem value="sol" label="Solidity">

```solidity
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.13;

contract Counter {
  // highlight-start
    uint public count;
  // highlight-end

    // Function to get the current count
    function get() public view returns (uint) {
        return count;
    }

    // Function to increment count by 1
    function inc() public {
        count += 1;
    }


    // Function to decrement count by 1
    function dec() public {
        // This function will fail if count = 0
        count -= 1;
    }
}
```

</TabItem>
  
<TabItem value="js" label="JavaScript">

```js
function addTwo(a, b) {
  return a + b;
}
```

</TabItem>

<TabItem value="jsx" label="jsx">

```jsx live
function Clock(props) {
  const [date, setDate] = useState(new Date());
  useEffect(() => {
    var timerID = setInterval(() => tick(), 1000);

    return function cleanup() {
      clearInterval(timerID);
    };
  });

  function tick() {
    setDate(new Date());
  }

  return (
    <div>
      <h2>It is {date.toLocaleTimeString()}.</h2>
    </div>
  );
}
```

</TabItem>

<TabItem value="ts" label="TypeScript">

```typescript
function add2(a, b) {
  return a + b;
}
```

</TabItem>

<TabItem value="py" label="Python">

```py
def add_two(a, b):
  return a + b;
```

</TabItem>
</Tabs>
